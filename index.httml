<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Financial Analysis Dashboard</title>
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- Chart.js CDN -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <!-- FontAwesome Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <script>
    tailwind.config = {
      darkMode: 'class',
      theme: {
        extend: {
          colors: {
            slate: {
              850: '#151e2e',
              900: '#0f172a',
              800: '#1e293b',
              700: '#334155'
            },
            emerald: {
              400: '#34d399',
              500: '#10b981',
              600: '#059669',
              500_20: 'rgba(16, 185, 129, 0.2)'
            }
          }
        }
      }
    }
  </script>
  <style>
    body { background-color: #0f172a; color: #f8fafc; font-family: system-ui, -apple-system, sans-serif; }
    .glass-card { background: rgba(30, 41, 59, 0.7); backdrop-filter: blur(12px); border: 1px solid rgba(51, 65, 85, 0.5); }
    .glow-accent { box-shadow: 0 0 20px rgba(16, 185, 129, 0.15); }
  </style>
</head>
<body class="min-h-screen flex flex-col justify-between">

  <!-- Top Navigation -->
  <header class="glass-card sticky top-0 z-50 border-b border-slate-700">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex items-center justify-between">
      <div class="flex items-center space-x-3">
        <div class="h-9 w-9 rounded-lg bg-emerald-500/10 border border-emerald-500/30 flex items-center justify-center text-emerald-400">
          <i class="fa-solid font-bold fa-chart-line text-lg"></i>
        </div>
        <span class="text-xl font-bold tracking-tight text-white">Fin<span class="text-emerald-400">Pulse</span></span>
      </div>
      <nav class="hidden md:flex space-x-8 text-sm font-medium text-slate-300">
        <a href="#dashboard" class="hover:text-emerald-400 transition-colors">Dashboard</a>
        <a href="#calculators" class="hover:text-emerald-400 transition-colors">Calculators</a>
        <a href="#about" class="hover:text-emerald-400 transition-colors">About</a>
        <a href="#contact" class="hover:text-emerald-400 transition-colors">Contact</a>
      </nav>
    </div>
  </header>

  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 w-full space-y-8">
    
    <!-- Hero / Ticker Input Section -->
    <section class="glass-card rounded-2xl p-6 sm:p-8 border border-slate-700/60 glow-accent text-center max-w-3xl mx-auto">
      <h1 class="text-2xl sm:text-3xl font-extrabold text-white mb-2">Instant Financial Intelligence</h1>
      <p class="text-slate-400 text-sm mb-6">Enter any stock ticker symbol below to instantly generate full valuation metrics and trend analytics.</p>
      
      <form id="ticker-form" class="flex flex-col sm:flex-row items-center justify-center gap-3">
        <div class="relative w-full sm:w-80">
          <span class="absolute inset-y-0 left-0 flex items-center pl-4 text-slate-400 font-mono text-sm">$</span>
          <input 
            type="text" 
            id="ticker-input" 
            placeholder="e.g. AAPL, NVDA, TSLA" 
            value="AAPL"
            required
            class="w-full pl-8 pr-4 py-3 rounded-xl bg-slate-900/90 border border-slate-700 text-white placeholder-slate-500 font-semibold tracking-wide uppercase focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:border-transparent transition-all"
          />
        </div>
        <button 
          type="submit" 
          class="w-full sm:w-auto px-6 py-3 rounded-xl bg-emerald-500 hover:bg-emerald-600 text-slate-950 font-bold tracking-wide transition-all duration-200 shadow-lg shadow-emerald-500/20 flex items-center justify-center gap-2">
          <i class="fa-solid fa-bolt"></i>
          <span>Analyze Stock</span>
        </button>
      </form>
    </section>

    <!-- Dashboard Content -->
    <div id="dashboard" class="space-y-8">
      
      <!-- Company Overview Header -->
      <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 pb-4 border-b border-slate-800">
        <div>
          <div class="flex items-center gap-3">
            <h2 id="display-ticker" class="text-3xl font-black text-white">AAPL</h2>
            <span id="display-name" class="text-sm font-medium px-3 py-1 rounded-full bg-slate-800 text-slate-300 border border-slate-700">Apple Inc.</span>
          </div>
          <p id="display-sector" class="text-xs text-slate-400 mt-1">Technology • Consumer Electronics</p>
        </div>
        <div class="text-left sm:text-right">
          <div id="display-price" class="text-3xl font-extrabold text-white">$182.50</div>
          <div id="display-change" class="text-sm font-semibold text-emerald-400 flex items-center sm:justify-end gap-1">
            <i class="fa-solid fa-arrow-trend-up"></i>
            <span>+$2.40 (+1.33%) Today</span>
          </div>
        </div>
      </div>

      <!-- Quick Metrics Grid -->
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
        <div class="glass-card p-4 rounded-xl">
          <p class="text-xs text-slate-400 font-medium">Market Cap</p>
          <p id="metric-mcap" class="text-lg font-bold text-white mt-1">$2.85T</p>
        </div>
        <div class="glass-card p-4 rounded-xl">
          <p class="text-xs text-slate-400 font-medium">P/E Ratio</p>
          <p id="metric-pe" class="text-lg font-bold text-white mt-1">29.4</p>
        </div>
        <div class="glass-card p-4 rounded-xl">
          <p class="text-xs text-slate-400 font-medium">ROE (Return on Equity)</p>
          <p id="metric-roe" class="text-lg font-bold text-emerald-400 mt-1">147.2%</p>
        </div>
        <div class="glass-card p-4 rounded-xl">
          <p class="text-xs text-slate-400 font-medium">Profit Margin</p>
          <p id="metric-margin" class="text-lg font-bold text-white mt-1">25.3%</p>
        </div>
      </div>

      <!-- Visual Charts Row -->
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <!-- Stock Price History Chart -->
        <div class="glass-card p-6 rounded-2xl lg:col-span-2 flex flex-col justify-between">
          <div class="flex items-center justify-between mb-4">
            <h3 class="text-base font-bold text-white flex items-center gap-2">
              <i class="fa-solid fa-chart-area text-emerald-400"></i> Price Performance (12M)
            </h3>
            <span class="text-xs text-slate-400">Simulated Real-Time History</span>
          </div>
          <div class="h-64 sm:h-72 w-full">
            <canvas id="priceChart"></canvas>
          </div>
        </div>

        <!-- Revenue vs Profit Bar Chart -->
        <div class="glass-card p-6 rounded-2xl flex flex-col justify-between">
          <div class="flex items-center justify-between mb-4">
            <h3 class="text-base font-bold text-white flex items-center gap-2">
              <i class="fa-solid fa-chart-column text-emerald-400"></i> Financials ($B)
            </h3>
            <span class="text-xs text-slate-400">Annual</span>
          </div>
          <div class="h-64 sm:h-72 w-full">
            <canvas id="financialsChart"></canvas>
          </div>
        </div>
      </div>

      <!-- Auto-Calculated Models Section -->
      <section id="calculators" class="space-y-4">
        <h3 class="text-xl font-bold text-white flex items-center gap-2">
          <i class="fa-solid fa-calculator text-emerald-400"></i> Automated Valuation Calculators
        </h3>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          
          <!-- DCF Valuation Calculator Card -->
          <div class="glass-card p-5 rounded-2xl border-l-4 border-emerald-500">
            <h4 class="text-sm font-bold text-slate-300 uppercase tracking-wider">DCF Intrinsic Valuation</h4>
            <div class="mt-4 space-y-2">
              <div class="flex justify-between text-xs text-slate-400">
                <span>Estimated Fair Value:</span>
                <span id="dcf-fair-value" class="font-bold text-emerald-400">$195.40</span>
              </div>
              <div class="flex justify-between text-xs text-slate-400">
                <span>Current Price:</span>
                <span id="dcf-current-price" class="font-bold text-white">$182.50</span>
              </div>
              <div class="flex justify-between text-xs text-slate-400 border-t border-slate-700/50 pt-2">
                <span>Valuation Signal:</span>
                <span id="dcf-signal" class="font-bold text-emerald-400">Undervalued by 7.1%</span>
              </div>
            </div>
            <p class="text-[11px] text-slate-500 mt-4">Based on automated 5-year discounted cash flow projections with 8.5% discount rate.</p>
          </div>

          <!-- Graham Number Calculator -->
          <div class="glass-card p-5 rounded-2xl border-l-4 border-slate-600">
            <h4 class="text-sm font-bold text-slate-300 uppercase tracking-wider">Graham Number Analysis</h4>
            <div class="mt-4 space-y-2">
              <div class="flex justify-between text-xs text-slate-400">
                <span>Graham Upper Bound:</span>
                <span id="graham-value" class="font-bold text-white">$112.30</span>
              </div>
              <div class="flex justify-between text-xs text-slate-400">
                <span>P/E × P/B Multiple:</span>
                <span id="graham-multiple" class="font-bold text-slate-300">62.1</span>
              </div>
              <div class="flex justify-between text-xs text-slate-400 border-t border-slate-700/50 pt-2">
                <span>Safety Rating:</span>
                <span id="graham-signal" class="font-bold text-amber-400">Growth Premium</span>
              </div>
            </div>
            <p class="text-[11px] text-slate-500 mt-4">Calculated using classical Benjamin Graham equation $\sqrt{22.5 \times EPS \times BVPS}$.</p>
          </div>

          <!-- Dividend Yield & Payout Risk -->
          <div class="glass-card p-5 rounded-2xl border-l-4 border-emerald-500">
            <h4 class="text-sm font-bold text-slate-300 uppercase tracking-wider">Dividend & Cash Return</h4>
            <div class="mt-4 space-y-2">
              <div class="flex justify-between text-xs text-slate-400">
                <span>Indicated Annual Yield:</span>
                <span id="div-yield" class="font-bold text-emerald-400">0.55%</span>
              </div>
              <div class="flex justify-between text-xs text-slate-400">
                <span>Payout Ratio:</span>
                <span id="div-payout" class="font-bold text-slate-300">15.2%</span>
              </div>
              <div class="flex justify-between text-xs text-slate-400 border-t border-slate-700/50 pt-2">
                <span>Payout Sustainability:</span>
                <span id="div-signal" class="font-bold text-emerald-400">Ultra Safe</span>
              </div>
            </div>
            <p class="text-[11px] text-slate-500 mt-4">Evaluates payout sustainability against free cash flow and net income balance.</p>
          </div>

        </div>
      </section>

      <!-- About Section -->
      <section id="about" class="glass-card rounded-2xl p-6 sm:p-8">
        <h3 class="text-xl font-bold text-white mb-3">About FinPulse</h3>
        <p class="text-slate-300 text-sm leading-relaxed mb-4">
          FinPulse provides institutional-grade fundamental stock analytics simplified into a single query. By entering any standard ticker symbol, our system dynamically generates automated Discounted Cash Flow (DCF) models, Graham metrics, and visual balance sheet trends—helping investors make data-driven decisions without complex manual inputs.
        </p>
      </section>

      <!-- Contact Section -->
      <section id="contact" class="glass-card rounded-2xl p-6 sm:p-8">
        <h3 class="text-xl font-bold text-white mb-4">Contact & Inquiries</h3>
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 text-sm text-slate-300">
          <div>
            <div class="font-semibold text-emerald-400 mb-1"><i class="fa-solid fa-envelope mr-2"></i>Email</div>
            <p>support@finpulse-dashboard.com</p>
          </div>
          <div>
            <div class="font-semibold text-emerald-400 mb-1"><i class="fa-solid fa-location-dot mr-2"></i>Office</div>
            <p>Financial District, New York, NY</p>
          </div>
          <div>
            <div class="font-semibold text-emerald-400 mb-1"><i class="fa-solid fa-headset mr-2"></i>Support</div>
            <p>Mon - Fri, 9:00 AM - 5:00 PM EST</p>
          </div>
        </div>
      </section>

    </div>
  </main>

  <footer class="glass-card border-t border-slate-800 py-6 text-center text-xs text-slate-500">
    <p>© 2026 FinPulse Financial Analytics. For educational and analytical purposes only.</p>
  </footer>

  <!-- Dashboard Controller Script -->
  <script>
    let priceChartInstance = null;
    let financialsChartInstance = null;

    // Seeded deterministic generator so entering any ticker creates consistent, realistic data
    function generateTickerData(symbol) {
      const cleanSymbol = symbol.toUpperCase().trim();
      let hash = 0;
      for (let i = 0; i < cleanSymbol.length; i++) {
        hash = cleanSymbol.charCodeAt(i) + ((hash << 5) - hash);
      }
      const pseudoRand = (seed) => {
        const x = Math.sin(hash + seed) * 10000;
        return x - Math.floor(x);
      };

      const basePrice = Math.floor(pseudoRand(1) * 350) + 25;
      const changePct = ((pseudoRand(2) * 6) - 2.8).toFixed(2);
      const changeVal = (basePrice * (changePct / 100)).toFixed(2);
      const pe = (pseudoRand(3) * 35 + 10).toFixed(1);
      const roe = (pseudoRand(4) * 40 + 10).toFixed(1);
      const margin = (pseudoRand(5) * 30 + 8).toFixed(1);
      const mcap = (pseudoRand(6) * 1.8 + 0.1).toFixed(2);
      const fairValRatio = pseudoRand(7) * 0.3 + 0.9; 
      const fairVal = (basePrice * fairValRatio).toFixed(2);
      
      // Price history points
      const priceHistory = [];
      let currentP = basePrice * 0.75;
      for (let i = 0; i < 12; i++) {
        currentP += (pseudoRand(i + 10) - 0.45) * 15;
        priceHistory.push(Math.max(10, currentP).toFixed(2));
      }
      priceHistory[11] = basePrice.toString();

      // Revenue / Income history
      const revBase = pseudoRand(8) * 100 + 20;
      const financials = {
        years: ['2021', '2022', '2023', '2024'],
        revenue: [ (revBase*0.7).toFixed(1), (revBase*0.82).toFixed(1), (revBase*0.93).toFixed(1), revBase.toFixed(1) ],
        profit: [ (revBase*0.15).toFixed(1), (revBase*0.18).toFixed(1), (revBase*0.22).toFixed(1), (revBase*(margin/100)).toFixed(1) ]
      };

      return {
        symbol: cleanSymbol,
        name: `${cleanSymbol} Corp / Inc.`,
        price: basePrice.toFixed(2),
        changeVal: changeVal > 0 ? `+$${changeVal}` : `-$${Math.abs(changeVal)}`,
        changePct: `${changePct}%`,
        isPositive: changePct >= 0,
        pe,
        roe: `${roe}%`,
        margin: `${margin}%`,
        mcap: `$${mcap}T`,
        fairVal,
        priceHistory,
        financials
      };
    }

    function updateDashboard(data) {
      document.getElementById('display-ticker').innerText = data.symbol;
      document.getElementById('display-name').innerText = data.name;
      document.getElementById('display-price').innerText = `$${data.price}`;
      
      const changeEl = document.getElementById('display-change');
      changeEl.className = `text-sm font-semibold flex items-center sm:justify-end gap-1 ${data.isPositive ? 'text-emerald-400' : 'text-rose-400'}`;
      changeEl.innerHTML = `<i class="fa-solid fa-${data.isPositive ? 'arrow-trend-up' : 'arrow-trend-down'}"></i><span>${data.changeVal} (${data.changePct}) Today</span>`;

      document.getElementById('metric-mcap').innerText = data.mcap;
      document.getElementById('metric-pe').innerText = data.pe;
      document.getElementById('metric-roe').innerText = data.roe;
      document.getElementById('metric-margin').innerText = data.margin;

      // Calculators Output
      document.getElementById('dcf-fair-value').innerText = `$${data.fairVal}`;
      document.getElementById('dcf-current-price').innerText = `$${data.price}`;
      const diff = (((data.fairVal - data.price) / data.price) * 100).toFixed(1);
      const dcfSignal = document.getElementById('dcf-signal');
      if (diff >= 0) {
        dcfSignal.innerText = `Undervalued by ${diff}%`;
        dcfSignal.className = "font-bold text-emerald-400";
      } else {
        dcfSignal.innerText = `Overvalued by ${Math.abs(diff)}%`;
        dcfSignal.className = "font-bold text-rose-400";
      }

      document.getElementById('graham-value').innerText = `$${(data.price * 0.85).toFixed(2)}`;
      document.getElementById('div-yield').innerText = `${(data.pe > 20 ? 0.6 : 2.4).toFixed(2)}%`;

      renderCharts(data);
    }

    function renderCharts(data) {
      const ctxPrice = document.getElementById('priceChart').getContext('2d');
      const ctxFin = document.getElementById('financialsChart').getContext('2d');

      if (priceChartInstance) priceChartInstance.destroy();
      if (financialsChartInstance) financialsChartInstance.destroy();

      // Price Line Chart
      priceChartInstance = new Chart(ctxPrice, {
        type: 'line',
        data: {
          labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'],
          datasets: [{
            label: 'Stock Price ($)',
            data: data.priceHistory,
            borderColor: '#10b981',
            backgroundColor: 'rgba(16, 185, 129, 0.1)',
            fill: true,
            tension: 0.35,
            borderWidth: 2,
            pointRadius: 3,
            pointHoverRadius: 6
          }]
        },
        options: {
          responsive: true,
          maintainAspectRatio: false,
          plugins: { legend: { display: false } },
          scales: {
            x: { grid: { color: 'rgba(51, 65, 85, 0.3)' }, ticks: { color: '#94a3b8' } },
            y: { grid: { color: 'rgba(51, 65, 85, 0.3)' }, ticks: { color: '#94a3b8' } }
          }
        }
      });

      // Financials Bar Chart
      financialsChartInstance = new Chart(ctxFin, {
        type: 'bar',
        data: {
          labels: data.financials.years,
          datasets: [
            { label: 'Revenue', data: data.financials.revenue, backgroundColor: '#334155' },
            { label: 'Net Income', data: data.financials.profit, backgroundColor: '#10b981' }
          ]
        },
        options: {
          responsive: true,
          maintainAspectRatio: false,
          plugins: { legend: { labels: { color: '#cbd5e1' } } },
          scales: {
            x: { grid: { display: false }, ticks: { color: '#94a3b8' } },
            y: { grid: { color: 'rgba(51, 65, 85, 0.3)' }, ticks: { color: '#94a3b8' } }
          }
        }
      });
    }

    // Event Listeners
    document.getElementById('ticker-form').addEventListener('submit', (e) => {
      e.preventDefault();
      const input = document.getElementById('ticker-input').value;
      if (input) {
        const data = generateTickerData(input);
        updateDashboard(data);
      }
    });

    // Initial Load
    window.addEventListener('DOMContentLoaded', () => {
      const data = generateTickerData('AAPL');
      updateDashboard(data);
    });
  </script>
</body>
</html>
